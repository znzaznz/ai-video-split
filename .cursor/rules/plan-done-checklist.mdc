---
description: Plan Build 完成前必须对照 _TEMPLATE 文末交付勾选逐项确认
alwaysApply: true
---

# Plan 完成清单（与 ship-workflow 配合）

## 何时执行

- **新建 Plan**：从 `.cursor/plans/_TEMPLATE.md` 复制，保留文末 **交付勾选** 整段；不得删掉该段。
- **Plan Build 结束** 或宣称「做完了」之前：必须对照勾选段逐项确认，并在 Plan 或回复中标明 `[x]` / 未完成项。
- **非 Plan 的单轮功能改动**：同样执行勾选（逻辑同 [ship-workflow.mdc](ship-workflow.mdc)）。

## Agent 必做（与勾选对应）

1. **分阶段 Build**：按 plan「阶段 1 → 验收 → 阶段 2」执行；**禁止**一次 Build 全部 TODO（见 [plan-generation-checklist.mdc](plan-generation-checklist.mdc)）。
2. **联动**：改切片/转写管线 → 检查 `gui.py`、`transcript_gui.py`、`transcript_cli.py` 等；删符号 → 全仓库 grep。
3. **验收**：每阶段结束 + 最终交付前跑 `scripts/verify.ps1`（失败则停止，不 commit、不宣称完成）。
4. **动手冒烟**：按 [execute-dont-preach.mdc](execute-dont-preach.mdc) **Agent 亲自跑** smoke/E2E，禁止只写教程。
5. **打包**：若动了打进 exe 的源码 → `build_exe.ps1` / `transcript_app/build_transcript_exe.ps1`；用户明确说不要 exe 可跳过。
6. **Git**：`git status` + staged 列表；禁止 `git add -A`；无 `.env`/exe/`runs/` 等；仅用户要求时 commit；不自动 push。
7. **关键决策**：实现偏离时在 `.cursor/plans/*.md` 追加「关键决策」行，勿只留在聊天里。

## 完整勾选列表

以 **`.cursor/plans/_TEMPLATE.md` 文末「交付勾选」** 为准；勿在规则里维护第二份冗长列表（避免两处不一致）。

## 例外

- 仅文档且用户说不要打包 → 可跳过 `build_exe.ps1`，仍建议跑 verify（若未改代码可跳过）。
- 用户说「先别提交」→ 做到 verify（+ 可选 build），不 commit。
