# 转写/切片输出
runs/

# 打包产物与中间文件
build/
dist/
*.exe
*.spec

# Python
__pycache__/
*.py[cod]
*$py.class

# 密钥与本地配置（只提交 .env.example）
.env
.env.local

# 运行时统计
usage_stats.json

# 编辑器（仅保留 .cursor/rules 可提交；Git 安全 hook 用用户全局 ~/.cursor）
.cursor/*
!.cursor/rules/